// import Header from '@/components/Header';

export default function LayoutCase({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* <Header /> */}
      {children}
    </div>
  );
}
